# -*- Makefile -*-
#
# @file  Makefile.VisionRecognitionTrigger# @brief RTComponent makefile for "VisionRecognitionTrigger component"
# @date  $Date$
#
# This file is generated by rtc-template with the following argments.
#
#  /usr/bin/rtc-template -bcxx --module-name=VisionRecognitionTrigger  \
#  --module-desc=Trigger recognition module --module-version=0.1  \
#  --module-vendor=AIST --module-category=Generic  \
#  --module-comp-type=DataFlowComponent --module-act-type=PERIODIC  \
#  --consumer=recogTrigger:recogTrigger:RecognitionService  \
#  --consumer-idl=RecognitionService.idl  
#
#
# $Id$
#
CXXFLAGS = `rtm-config --cflags` -I.
LDFLAGS  = `rtm-config --libs`
SHFLAGS  = -shared

IDLC     = `rtm-config --idlc`
IDLFLAGS = `rtm-config --idlflags` -I`rtm-config --prefix`/include/rtm/idl
WRAPPER  = rtm-skelwrapper
WRAPPER_FLAGS = --include-dir="" --skel-suffix=Skel --stub-suffix=Stub

SKEL_OBJ =  
STUB_OBJ = RecognitionServiceStub.o  
IMPL_OBJ =  
OBJS     = VisionRecognitionTrigger.o $(SKEL_OBJ) $(STUB_OBJ) $(IMPL_OBJ)

.SUFFIXES : .so

all: VisionRecognitionTrigger.so VisionRecognitionTriggerComp


.cpp.o:
	rm -f $@
	$(CXX) $(CXXFLAGS) -c -o $@ $<

.o.so:
	rm -f $@
	$(CXX) $(SHFLAGS) -o $@ $(OBJS) $(LDFLAGS)

VisionRecognitionTriggerComp: VisionRecognitionTriggerComp.o $(OBJS)
	$(CXX) -o $@ $(OBJS) VisionRecognitionTriggerComp.o $(LDFLAGS) 


clean: clean_objs clean_skelstub
	rm -f *~

clean_objs:
	rm -f $(OBJS) VisionRecognitionTriggerComp.o VisionRecognitionTrigger.so VisionRecognitionTriggerComp

clean_skelstub:
	rm -f *Skel.h *Skel.cpp
	rm -f *Stub.h *Stub.cpp


RecognitionServiceStub.cpp : RecognitionService.idl 
	$(IDLC) $(IDLFLAGS) RecognitionService.idl 
	$(WRAPPER) $(WRAPPER_FLAGS) --idl-file=RecognitionService.idl 
RecognitionServiceStub.h : RecognitionService.idl 
	$(IDLC) $(IDLFLAGS) RecognitionService.idl 
	$(WRAPPER) $(WRAPPER_FLAGS) --idl-file=RecognitionService.idl 

VisionRecognitionTrigger.so: $(OBJS)
VisionRecognitionTrigger.o: VisionRecognitionTrigger.h RecognitionServiceStub.h  
VisionRecognitionTriggerComp.o: VisionRecognitionTriggerComp.cpp VisionRecognitionTrigger.cpp VisionRecognitionTrigger.h  


RecognitionServiceSkel.o: RecognitionServiceSkel.cpp RecognitionServiceSkel.h RecognitionServiceStub.h
RecognitionServiceStub.o: RecognitionServiceStub.cpp RecognitionServiceStub.h

# end of Makefile

